<ul class="nav nav-tabs">
  <li> <%= link_to tag_icon(:list, pat(:list)), url(:votes, :index) %></li>
  <li class="active"> <%= link_to tag_icon(:plus, pat(:new)), url(:votes, :new) %></li>
</ul>
<div class="tabs-content">
  <% form_for :vote, url(:votes, :create), :class => 'form-horizontal' do |f| %>
    <%= partial 'votes/form', :locals => { :f => f } %>
 <% end %>
</div>
